% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord.R
\name{add_x_pos}
\alias{add_x_pos}
\alias{add_index}
\title{Add Cumulative Character Lengths to a Data Frame
Adds a new column \code{x_start} and \code{x_end} to a data frame, representing the cumulative sum of character lengths within specified groups.}
\usage{
add_x_pos(df, group = "sentence", base = "word")

add_index(df, group = "sentence")
}
\arguments{
\item{df}{A data frame containing sentences.}

\item{group}{A string of the column name containing sentence identifiers.}

\item{base}{A string of the column name containing the base words for character length calculations.}
}
\value{
The modified data frame with the added \code{x_start} and \code{x_end} columns.

The modified data frame with the added \code{index} column.
}
\description{
Add Cumulative Character Lengths to a Data Frame
Adds a new column \code{x_start} and \code{x_end} to a data frame, representing the cumulative sum of character lengths within specified groups.

Add index to a Data Frame
Adds a new column \code{index} to a data frame, representing the cumulative sum of character lengths within specified groups.
}
\examples{
sentences <- list(
  c("the", "quick", "brown", "fox"),
  c("the", "lazy", "dog"),
  c("jumped", "over", "the", "moon")
)
df <- list2df(sentences)
df <- add_x_pos(df, group = "sentence", base = "word")
df <- add_index(df)

}
